# Supabase — get from dashboard.supabase.com → Settings → API
SUPABASE_URL=https://dhbegdhoyhnrwmoktomi.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJ...  # ← service_role key, NOT the anon key

# Railway injects PORT automatically — do not define locally
# PORT=3000

# Optional — Railway also injects this automatically
# RAILWAY_PUBLIC_URL=https://cityhealth-mcp.railway.app

# MCP auth mode (set to false for public connector access)
MCP_REQUIRE_API_KEY=false
